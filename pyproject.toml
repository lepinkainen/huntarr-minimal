[project]
name = "huntarr-minimal"
version = "0.1.0"
description = "Minimal Sonarr/Radarr missing + upgrade search automation"
requires-python = ">=3.10"
dependencies = [
  "requests>=2.31.0",
  "pyyaml>=6.0.1",
]

[tool.uv]
package = false
